# Инструкция по использованию логов распределения

## Где находятся логи

Все логи сохраняются в папке: `/Users/oleg/Project_SKLAD/logs/`

Каждый запуск создает новый файл с timestamp:
```
allocation_20251129_203000.log
allocation_20251129_204500.log
```

## Что записывается в логи

### 1. Общая информация о загрузке данных
```
🚀 НАЧАЛО РАСПРЕДЕЛЕНИЯ АУКЦИОНА
Продажи загружены: 1523 строк, окно 12 нед.
Уникальных подразделений: 15
Уникальных регионов: 3
Лотов в реестре: 245 (уникальных SKU: 87)
```

### 2. Детальное распределение каждого SKU
```
🎯 Начинаем распределение SKU: 12345678901, количество: 5 шт
  📦 Доступно подразделений-кандидатов: 8

  --- Итерация 1, осталось: 5 шт ---
  🏆 Топ-3 кандидатов по скору:
    #1: москва-1 - score=12.50, velocity=5.2, coverage=0.4н
    #2: спб-1 - score=8.30, velocity=3.1, coverage=2.1н
    #3: казань-1 - score=5.10, velocity=2.0, coverage=4.5н
  
  ✅ Штука 1/5 → москва-1: скор=12.50, velocity=5.2/нед, покрытие=0.4н, p≤30д≈85.5%, маржа≈2500
```

### 3. Итоговая статистика
```
📊 ИТОГОВАЯ СТАТИСТИКА
Всего распределено: 245 штук
Уникальных SKU: 87
Подразделений задействовано: 12

Топ-5 подразделений по количеству:
  1. москва-1 (москва): 48 шт (19.6%)
  2. спб-1 (санкт-петербург): 42 шт (17.1%)
  3. казань-1 (казань): 35 шт (14.3%)
```

## Как анализировать логи

### Проблема: Все идет в одно подразделение

**Ищем в логе:**
```
Топ-5 подразделений по количеству:
  1. москва-1: 150 шт (61.2%)  ← ⚠️ БОЛЬШЕ 50%!
```

**Что смотреть:**
- Проверьте скоры в "Топ-3 кандидатов"
- Если у победителя score сильно выше (в 2+ раза) → это нормально, там реально лучше продается
- Если score примерно равны → увеличьте `fairness_penalty`

### Проблема: Неочевидное распределение

**Пример из лога:**
```
  ✅ Штука 3/5 → казань-1: скор=5.10, velocity=2.0/нед, покрытие=4.5н
```

**Расшифровка:**
- `скор=5.10` - итоговый балл (выше = лучше)
- `velocity=2.0/нед` - продают 2 штуки в неделю
- `покрытие=4.5н` - остаток на 4.5 недели (близко к target=4)
- `p≤30д≈65%` - вероятность продажи за месяц
- `маржа≈2500` - ожидаемая маржа

### Проблема: Предупреждения в логах

**⚠️ Данные продаж охватывают только 3 дней**
→ Скорость продаж неточная, добавьте больше исторических данных

**❌ Совсем нет данных для SKU**
→ Артикул новый, нет истории продаж

**⚠️ Нет истории по SKU, используем топ подразделений**
→ По этому товару нет продаж, распределяем по общей скорости

## Команды для анализа логов

### Найти последний лог
```bash
ls -lt /Users/oleg/Project_SKLAD/logs/ | head -5
```

### Просмотреть итоговую статистику
```bash
grep "ИТОГОВАЯ СТАТИСТИКА" -A 20 /Users/oleg/Project_SKLAD/logs/allocation_*.log | tail -25
```

### Найти проблемные распределения (>40% в одном месте)
```bash
grep "Топ-5" -A 5 /Users/oleg/Project_SKLAD/logs/allocation_*.log | grep -E "[4-9][0-9]\.[0-9]%|100"
```

### Посмотреть все предупреждения
```bash
grep "WARNING\|⚠️\|❌" /Users/oleg/Project_SKLAD/logs/allocation_*.log
```

### Найти распределение конкретного SKU
```bash
grep "SKU: 12345678901" -A 50 /Users/oleg/Project_SKLAD/logs/allocation_*.log
```

## Открыть лог в редакторе

```bash
# Последний лог
code $(ls -t /Users/oleg/Project_SKLAD/logs/*.log | head -1)

# Или просто
open /Users/oleg/Project_SKLAD/logs/
```

## Что делать при проблемах

### 1. Слишком неравномерное распределение
**В логе видим:** `москва-1: 120 шт (48.8%)`

**Решение:** 
- Увеличьте `fairness_penalty` с 0.5 до 1.0
- Уменьшите `coverage_weight` 

### 2. Игнорируется скорость продаж
**В логе видим:** Товары идут в медленные точки с высоким дефицитом

**Решение:**
- Увеличьте `velocity_weight` с 0.2 до 1.0
- Уменьшите `coverage_weight`

### 3. Много предупреждений о малом окне данных
**В логе:** `⚠️ Данные продаж охватывают только 3 дней`

**Решение:**
- Используйте файл продаж за больший период
- Минимум 7-14 дней рекомендуется
