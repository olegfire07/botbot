# 🚀 Инструкция по развертыванию (Deployment Guide)

Этот проект упакован в Docker для легкого запуска на любом компьютере или сервере (VPS).

## 📋 Требования

1. **Docker** и **Docker Compose** должны быть установлены на целевой машине.
   - [Инструкция для Windows/Mac](https://www.docker.com/products/docker-desktop)
   - [Инструкция для Ubuntu](https://docs.docker.com/engine/install/ubuntu/)

## 🛠 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone https://github.com/olegfire07/botbot.git
cd botbot
```

### 2. Создание .env файла
Создайте файл `.env` в корне проекта со следующим содержимым:

```env
BOT_TOKEN=ваш_токен_от_BotFather
MAIN_GROUP_CHAT_ID=ваш_id_группы
DEFAULT_ADMIN_IDS=ваш_telegram_id
```

**Где взять эти данные:**
- `BOT_TOKEN` — получите у [@BotFather](https://t.me/BotFather) в Telegram
- `MAIN_GROUP_CHAT_ID` — ID группы для отправки заключений (можно узнать у [@userinfobot](https://t.me/userinfobot))
- `DEFAULT_ADMIN_IDS` — ваш Telegram ID (можно узнать у [@userinfobot](https://t.me/userinfobot))

### 3. Создание необходимых папок
```bash
mkdir -p logs backups config documents documents_archive photos
```

### 4. Запуск через Docker Compose
```bash
docker-compose up -d --build
```

Эта команда:
- Соберет образ (установит Python 3.11 и все библиотеки).
- Запустит контейнер в фоновом режиме (`-d`).
- Пробросит порт `8080` для API.
- Автоматически создаст базу данных при первом запуске.

### 5. Проверка работы
Посмотрите логи, чтобы убедиться, что бот запустился:

```bash
docker-compose logs -f
```

Должно появиться сообщение: `Bot started.`

### 6. Остановка и обновление

**Остановка:**
```bash
docker-compose down
```

**Обновление кода и перезапуск:**
```bash
git pull
docker-compose up -d --build
```

**Просмотр логов:**
```bash
docker-compose logs -f bot
```

**Перезапуск без пересборки:**
```bash
docker-compose restart
```

## 📊 Структура данных

Docker использует volumes для хранения важных данных:
- `bot_data` — SQLite база данных пользователей
- `bot_persistence` — состояние бота (pickle файл)
- `./logs` — логи работы бота
- `./backups` — автоматические бэкапы БД
- `./documents` — сгенерированные документы
- `./photos` — загруженные фото

**Важно:** При удалении контейнера (`docker-compose down`) данные в volumes НЕ удаляются.

Для полной очистки:
```bash
docker-compose down -v  # Удалит volumes
```

## ☁️ Cloudflare Workers vs Docker

Вы спрашивали про **Cloudflare Workers**.
Этот проект **НЕЛЬЗЯ** запустить на Cloudflare Workers в текущем виде, потому что:

1. **Длительный процесс:** Бот использует `polling` (постоянно опрашивает Telegram), а Workers — это функции, которые живут миллисекунды.
2. **Файловая система:** Бот пишет файлы (`user_data.db`, логи, фото) на диск. У Workers нет доступа к диску.
3. **Python:** Workers лучше всего работают с JavaScript/WASM. Поддержка Python там ограничена.

**Решение:** Используйте **Docker** на любом дешевом VPS (например, Ubuntu за $5/мес на DigitalOcean, Hetzner, или Contabo). Это самый надежный и простой способ.

## 🔧 Troubleshooting

**Проблема:** `address already in use`
**Решение:** Порт 8080 занят. Измените в `docker-compose.yml`:
```yaml
ports:
  - "8081:8080"  # Используйте другой внешний порт
```

**Проблема:** Бот не отвечает
**Решение:** Проверьте логи:
```bash
docker-compose logs -f
```

**Проблема:** База данных повреждена
**Решение:** Восстановите из бэкапа:
```bash
cp backups/user_data_YYYY-MM-DD.db user_data.db
docker-compose restart
```

## 📞 Поддержка

Если возникли вопросы, создайте Issue в репозитории GitHub.
