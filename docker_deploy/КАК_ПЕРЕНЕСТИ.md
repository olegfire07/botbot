# 📦 Как перенести бота на другой компьютер

## Вариант 1: Архивирование (самый простой)

### На текущем компьютере:

```bash
# Перейдите в родительскую папку
cd /Users/oleg/Project_SKLAD

# Создайте ZIP-архив
zip -r bot_deploy.zip docker_deploy/ -x "*.pyc" -x "*__pycache__*" -x "*.DS_Store"

# Или TAR.GZ (меньше размер)
tar -czf bot_deploy.tar.gz docker_deploy/ --exclude="*.pyc" --exclude="__pycache__" --exclude=".DS_Store"
```

### На новом компьютере:

```bash
# Распакуйте архив
unzip bot_deploy.zip
# или
tar -xzf bot_deploy.tar.gz

# Перейдите в папку
cd docker_deploy

# Запустите бота
docker-compose up -d
```

## Вариант 2: Через USB или облако

1. **Скопируйте всю папку** `docker_deploy` на флешку/облако
2. **Перенесите** на новый компьютер
3. **Запустите** `docker-compose up -d`

## Вариант 3: Через Git (если используете)

```bash
# На текущем компьютере
cd docker_deploy
git init
git add .
git commit -m "Initial bot deployment"
git remote add origin YOUR_REPO_URL
git push -u origin main

# На новом компьютере
git clone YOUR_REPO_URL
cd docker_deploy
docker-compose up -d
```

⚠️ **ВАЖНО**: Если используете Git, добавьте `.env` в `.gitignore` и настройте его вручную на новом компьютере!

## ✅ Проверочный список перед переносом

- [ ] Файл `.env` заполнен правильными данными
- [ ] На новом компьютере установлен Docker
- [ ] Порт 8080 свободен на новом компьютере
- [ ] Все файлы скопированы (или архив создан)

## 🎯 Быстрый старт на новом компьютере

```bash
# 1. Распакуйте/перенесите папку docker_deploy
# 2. Откройте терминал в папке docker_deploy
cd docker_deploy

# 3. Проверьте .env
cat .env

# 4. Запустите
docker-compose up -d

# 5. Проверьте статус
docker-compose logs -f
```

**Готово! Бот работает! 🚀**

---

Подробные инструкции смотрите в файле [README_DEPLOY.md](README_DEPLOY.md)
