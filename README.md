# 🤖 Telegram Bot - Conclusion Generator

Telegram бот для создания заключений с веб-интерфейсом. Поддерживает создание документов через Web App, рассылки с изображениями, автоматическую архивацию и Excel отчеты.

**Версия:** 3.5.0

## 📋 Содержание

- [Возможности](#возможности)
- [Быстрый старт](#быстрый-старт)
- [Установка на новом компьютере](#установка-на-новом-компьютере)
- [Docker развертывание](#docker-развертывание)
- [Конфигурация](#конфигурация)
- [Управление](#управление)

## ✨ Возможности

### Основные
- 📝 **Web App** - Современный интерфейс для создания заключений
- 🖼️ **Поддержка изображений** - Загрузка и сжатие фото предметов
- 📊 **Excel отчеты** - Автоматическое обновление базы в Excel
- 🗂️ **Архивация** - Сохранение документов по месяцам
- 📢 **Рассылки** - Broadcast сообщений с изображениями

### Для администраторов
- 👥 **Управление пользователями** - Добавление/удаление доступа
- 📈 **Статистика** - Отчеты по заключениям
- 🔍 **Поиск билетов** - Быстрый поиск по номеру
- 💾 **Бэкапы** - Автоматическое резервное копирование БД
- ⚙️ **Сверка** - Импорт Excel для проверки данных

### Технические
- 🔒 **Защита от дубликатов** - Lock file предотвращает множественный запуск
- 🛡️ **HTML экранирование** - Безопасная рассылка сообщений
- 🔄 **Автоперезапуск** - Systemd service для стабильности
- 🌐 **API endpoint** - REST API для Web App
- 📍 **Топики Telegram** - Сортировка по регионам

---

## 🚀 Быстрый старт

### Требования
- Python 3.11+
- Telegram Bot Token (от @BotFather)
- ImgBB API Key (для хранения фото)

### За 5 минут

```bash
# 1. Клонировать репозиторий
git clone https://github.com/olegfire07/botbot.git
cd botbot

# 2. Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Настроить .env
cp .env.example .env
nano .env  # Добавить BOT_TOKEN, IMGBB_KEY, ADMIN_IDS

# 5. Запустить
python run_modern_bot.py
```

✅ Готово! Бот запущен.

---

## 💻 Установка на новом компьютере

### Вариант 1: С нуля (рекомендуется)

#### Шаг 1: Подготовка системы

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install -y python3.11 python3.11-venv python3-pip git
```

**macOS:**
```bash
brew install python@3.11 git
```

**Windows:**
1. Скачать Python 3.11+ с [python.org](https://www.python.org/downloads/)
2. Установить Git с [git-scm.com](https://git-scm.com/download/win)

#### Шаг 2: Клонирование проекта

```bash
# Создать папку для проекта
mkdir -p ~/bots
cd ~/bots

# Клонировать репозиторий
git clone https://github.com/olegfire07/botbot.git
cd botbot
```

#### Шаг 3: Установка зависимостей

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установить пакеты
pip install --upgrade pip
pip install -r requirements.txt
```

#### Шаг 4: Конфигурация

```bash
# Скопировать шаблон настроек
cp .env.example .env

# Открыть редактор
nano .env  # или vim, code, notepad++
```

**Обязательные параметры в .env:**

```env
# Telegram Bot Token (получить у @BotFather)
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Admin IDs (ваш Telegram ID, узнать у @userinfobot)
ADMIN_IDS=2064900,123456789

# ImgBB API Key (бесплатно на imgbb.com/api)
IMGBB_KEY=abc123def456

# URL бота (для веб-приложения, опционально)
BOT_URL=http://localhost:8080

# API включен по умолчанию
API_ENABLED=true
API_PORT=8080
```

#### Шаг 5: Запуск

```bash
# Обычный запуск
python run_modern_bot.py

# Или в фоне (Linux/Mac)
nohup python run_modern_bot.py > bot.log 2>&1 &
```

**Проверка работы:**
```bash
# Открыть в браузере
curl http://localhost:8080/health

# Должен вернуть:
# {"status":"ok","bot":"running","database":"connected","uptime":"0h 1m","version":"3.5.0"}
```

---

### Вариант 2: Из бэкапа

Если у вас есть папка `bot_backup_2025-12-02`:

```bash
# 1. Скопировать бэкап на новый компьютер
scp -r bot_backup_2025-12-02 user@new-server:~/

# 2. На новом сервере
cd ~/bot_backup_2025-12-02

# 3. Создать venv и установить зависимости
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 4. Проверить/обновить .env
nano .env

# 5. Запустить
python run_modern_bot.py
```

---

## 🐳 Docker развертывание

### Быстрый запуск

```bash
# 1. Настроить .env
cp .env.example .env
nano .env  # Добавить токены

# 2. Запустить через Docker Compose
docker-compose up -d

# 3. Проверить статус
docker-compose ps
docker-compose logs -f
```

### Ручная сборка

```bash
# Собрать образ
docker build -t telegram-bot:latest .

# Запустить контейнер
docker run -d \
  --name telegram-bot \
  --env-file .env \
  -p 8080:8080 \
  -v $(pwd)/user_data.db:/app/user_data.db \
  -v $(pwd)/logs:/app/logs \
  telegram-bot:latest

# Проверить логи
docker logs -f telegram-bot
```

### Docker Compose опции

```yaml
# docker-compose.yml уже настроен
# Команды управления:

# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Просмотр логов
docker-compose logs -f

# Обновление
git pull
docker-compose build
docker-compose up -d
```

---

## ⚙️ Конфигурация

### Структура .env файла

```env
# === ОБЯЗАТЕЛЬНО ===
BOT_TOKEN=your_bot_token_here
ADMIN_IDS=123456789,987654321
IMGBB_KEY=your_imgbb_key_here

# === API ===
API_ENABLED=true
API_PORT=8080
API_BIND_HOST=127.0.0.1
API_AUTH_TOKEN=  # Опционально для защиты API

# === URLs ===
BOT_URL=http://localhost:8080
ALLOWED_ORIGINS=https://olegfire07.github.io

# === Лимиты ===
API_MAX_REQUEST_SIZE_MB=2
```

### Получение токенов

**1. Bot Token:**
```
1. Открыть @BotFather в Telegram
2. Отправить /newbot
3. Следовать инструкциям
4. Скопировать токен
```

**2. IMGBB_KEY:**
```
1. Зарегистрироваться на imgbb.com
2. Перейти в API Settings
3. Создать API Key
4. Скопировать ключ
```

**3. Admin ID:**
```
1. Отправить /start боту @userinfobot
2. Скопировать ваш ID
3. Добавить в ADMIN_IDS
```

---

## 🎛️ Управление

### Systemd (Linux - рекомендуется)

```bash
# Установить service
sudo cp telegram-bot.service /etc/systemd/system/
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable telegram-bot.service

# Управление
sudo systemctl start telegram-bot     # Запустить
sudo systemctl stop telegram-bot      # Остановить
sudo systemctl restart telegram-bot   # Перезапустить
sudo systemctl status telegram-bot    # Статус

# Логи
sudo journalctl -u telegram-bot -f
```

### PM2 (альтернатива)

```bash
# Установить PM2
npm install -g pm2

# Запустить бота
pm2 start run_modern_bot.py --name telegram-bot --interpreter python3

# Управление
pm2 stop telegram-bot
pm2 restart telegram-bot
pm2 logs telegram-bot
pm2 monit

# Автозапуск
pm2 startup
pm2 save
```

### Обновление

```bash
# Git pull
git pull origin main

# Перезапуск (systemd)
sudo systemctl restart telegram-bot

# Или (PM2)
pm2 restart telegram-bot

# Или (вручную)
pkill -f run_modern_bot.py
python run_modern_bot.py
```

---

## 🔧 Администрирование

### Команды бота

- `/start` - Главное меню
- `/admin` - Админ-панель
- `/help` - Справка
- `/stats` - Статистика за месяц
- `/history` - История заключений

### Админ-панель функции

- 📊 **Статистика** - Просмотр метрик
- 👥 **Пользователи** - Управление доступом
- 📢 **Рассылка** - Broadcast сообщения
- 🔍 **Поиск** - Найти заключение по билету
- 💾 **Бэкап** - Скачать/восстановить БД
- ⚙️ **Сверка** - Импорт Excel для проверки

---

## 📁 Структура проекта

```
prod bot/
├── modern_bot/           # Основной код бота
│   ├── handlers/         # Обработчики команд
│   ├── services/         # Бизнес-логика
│   ├── database/         # SQLite работа
│   └── utils/            # Вспомогательные функции
├── web_app/              # Web интерфейс
│   └── index.html        # SPA приложение
├── config/               # Конфигурация
├── logs/                 # Логи (создается автоматически)
├── .env                  # Переменные окружения
├── requirements.txt      # Python зависимости
├── run_modern_bot.py     # Точка входа
├── template.docx         # Шаблон документа
└── README.md             # Эта инструкция
```

---

## 🐛 Решение проблем

### Бот не запускается

```bash
# Проверить .env
cat .env | grep BOT_TOKEN

# Проверить зависимости
pip install -r requirements.txt

# Проверить логи
tail -f logs/bot.log
```

### API не отвечает

```bash
# Проверить порт
netstat -tuln | grep 8080
# или
lsof -i :8080

# Проверить health
curl http://localhost:8080/health
```

### Web App не грузится

```bash
# Проверить версию в браузере (добавить ?v=3.5)
https://olegfire07.github.io/botbot/web_app/?v=3.5

# Очистить кеш в Telegram
Settings > Data and Storage > Clear cache
```

### Дублирующие процессы

```bash
# Найти процессы
ps aux | grep run_modern_bot

# Убить все
pkill -f run_modern_bot.py

# Проверить lockfile
ls -la .bot.lock

# Удалить если завис
rm .bot.lock
```

---

## 📞 Поддержка

Если возникли вопросы:
1. Проверьте логи: `tail -f logs/bot.log`
2. Проверьте health: `curl localhost:8080/health`
3. Убедитесь что .env настроен правильно
4. Попробуйте перезапустить бота

---

## 🔄 Changelog

### v3.5.0 (2025-12-02)
- ✅ Добавлена валидация дат (нельзя выбрать будущие)
- ✅ HTML-экранирование в рассылке
- ✅ Номер заключения в сообщениях группы
- ✅ Systemd service для автоуправления
- ✅ Версионирование бота
- 🔧 Исправлены голые except блоки
- 🔧 Улучшен rate limiting рассылки
- 🔧 Lock file защита от дубликатов

---

**Разработано:** Oleg  
**Лицензия:** Проприетарная  
**Версия:** 3.5.0
